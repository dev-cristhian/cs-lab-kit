---
'cs-lab-kit': minor
---

Add `CPF` and `CNPJ` classes with `isValid`, `format`, `clean`, and `generate` methods. CNPJ also supports alphanumeric generation (new Brazilian Federal Revenue format).
